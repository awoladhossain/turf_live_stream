# .golangci.yml
version: "2"

run:
  timeout: 5m
  issues-exit-code: 1
  tests: true

linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - unused
    - gosimple
    - goimports
    - revive

issues:
  max-issues-per-linter: 0
  max-same-issues: 0
